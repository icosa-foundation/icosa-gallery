[pytest]
DJANGO_SETTINGS_MODULE = django_project.test_settings
python_files = tests.py test_*.py *_tests.py
python_classes = Test*
python_functions = test_*
testpaths = icosa/tests
addopts =
    --reuse-db
    --cov=icosa
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    --verbose
    --strict-markers
    --tb=short
    --maxfail=5
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    unit: marks tests as unit tests
    api: marks tests as API tests
    models: marks tests as model tests
    views: marks tests as view tests
    helpers: marks tests as helper tests
    forms: marks tests as form tests
    commands: marks tests as management command tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
